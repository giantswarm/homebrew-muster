# Homebrew tap for muster

[muster](https://github.com/giantswarm/muster): one MCP endpoint for every MCP server your
platform runs.

## Install

Homebrew loads formulae from a third-party tap only after the tap has been trusted:

```bash
brew trust giantswarm/muster
brew install giantswarm/muster/muster
```

The formula installs the release binary for macOS and Linux on `amd64` and `arm64`, with shell
completions for bash, zsh and fish.

## Upgrade

```bash
brew upgrade muster
```

## How the formula stays current

`Formula/muster.rb` is generated by [`scripts/update-formula.sh`](scripts/update-formula.sh).
Do not edit it by hand.

- The release pipeline of giantswarm/muster sends this repository a `muster-release` dispatch
  once a release's binaries are uploaded
  ([`.circleci/custom.yml`](https://github.com/giantswarm/muster/blob/main/.circleci/custom.yml)).
  The [Update formula](.github/workflows/update-formula.yaml) workflow verifies every binary
  against its Sigstore bundle (a CircleCI build of giantswarm/muster, the identity
  `muster self-update` checks), installs the formula on the runner, then opens a pull request
  and merges it.
- [Check for a new release](.github/workflows/check-release.yaml) runs the same update every
  six hours in case a dispatch was lost.
- The pull requests are opened and merged by heraldbot, Giant Swarm's automation App, which
  the `protect-main` ruleset lists as a bypass actor. Pull requests by people still need a
  review and pass the [Test formula](.github/workflows/test-formula.yaml) check.
- By hand: `gh workflow run update-formula.yaml --repo giantswarm/homebrew-muster`, with
  `-f tag=v5.22.0` to pin a release. Locally: `scripts/update-formula.sh`, then
  `scripts/test-formula.sh`.

## Credits

Original Homebrew formula contributed by [@choas](https://github.com/choas) (Lars).

## License

Apache-2.0
